#include <stdio.h>
#include <unistd.h>
#include <string.h>
#include <stdbool.h>

char input_buffer[256];

void error(char* message) {
    printf("msh error: %s\n", message);
}

int main(int argc, char* argv) {
    while(true) {
        printf("msh-0.01\n")
    }

    printf("/home/matthew/ ~>")

    return 0;
}
